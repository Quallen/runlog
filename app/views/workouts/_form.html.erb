<%= form_for(@workout) do |f| %>

<% if @workout.errors.any? %>

  <div id="error_explanation">
   <h2><%= pluralize(@workout.errors.count, "error") %> prohibited this run from being saved:</h2>

   <ul>
     <% @workout.errors.full_messages.each do |msg| %>
       <li><%= msg %></li>
     <% end %>
   </ul>
  </div>

<% end %>

  <%= f.label :date %>
  <%= f.text_field :date %>

  <%= f.label :distance %>
  <%= f.number_field :distance, :step => ".01" %>

  <%= f.label :effort %>
  <%= f.select :effort, Workout::EFFORT_LIST, :include_blank => true %>

  <%= f.label :note %>
  <%= f.text_field :note %>

<%= f.submit @workout.new_record? ? "Submit" : "Update" %>

<% end %>
