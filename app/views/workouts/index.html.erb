<h1>Workout#index</h1>
<p>Find me in app/views/workout/index.html.erb</p>

<p> Hello <%= current_user.email %> </p>

<p> List view workout data for now. Would like to graph things though</p>

<p> Rolling 7 day total mileage:  <%= number_with_precision(@rolling_total , precision: 1) %> </p>
<p> Last weeks total mileage: <%= number_with_precision(@last_week_total, precision: 1) %> </p>
<table>
  <caption>Table of logged runs</caption>
  <thead>
    <tr>
      <th>Date</th>
      <th>Distance</th>
      <th>Effort</th>
      <th>Notes</th>
    </tr>
  </thead>
  <tbody>
    <% @workouts.each do |run| %>
      <tr>
        <td><%= run.date.strftime('%m/%d/%Y') %></td>
        <td><%= run.distance %></td>
        <td><%= run.effort %></td>
        <td><%= truncate(run.note, length: 60) %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<%= link_to "Log New Run", new_workout_path(current_user), class: "button" %>
